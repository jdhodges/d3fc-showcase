import insertAxisBackground from './insertAxisBackground';
import reorderAxis from './reorderAxis';
import repositionAxis from './repositionAxis';

export default {
    insertAxisBackground: insertAxisBackground,
    reorderAxis: reorderAxis,
    repositionAxis: repositionAxis
};
